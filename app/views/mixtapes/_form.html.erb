<%= form_for(@mixtape) do |f| %>
  <% if @mixtape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mixtape.errors.count, "error") %> prohibited this mixtape from being saved:</h2>

      <ul>
      <% @mixtape.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <% Song.all.each do |song| %>
    <%= song.name %>
    <%= check_box_tag "mixtape[song_ids][]", song.id, @mixtape.songs.include?(song) %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
